spring:
  profiles:
    active: local
  application:
    name: gym-service-client

server:
  port: 8886

gym:
  service:
    baseUrl: 'http://localhost:8585/api/training-session'
    searchSession: '/search'